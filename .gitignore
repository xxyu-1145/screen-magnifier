# Build output and the packaged artifact. `./build.sh clean` removes build/;
# dist/ is regenerated by `./build.sh package`. Both come from src/ and tests/
# alone.
build/
dist/

# Per-machine Claude Code settings, not part of the project.
.claude/
